import { Metadata } from "next"

export const metadata: Metadata = {
    title: {
        absolute: "Profile"
    }
}

const page = () => {
    return (
        <div>Profile</div>
    )
}

export default page